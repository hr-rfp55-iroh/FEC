import React from 'react';

const RatingBreakdown = (props) => {
  const test = 'Rating Breakdown Here';
  return (
    <div className="rating-breakdown">{test}</div>
  );
};

export default RatingBreakdown;
